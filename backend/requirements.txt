# FastAPI and server
fastapi[standard]>=0.115.0
uvicorn[standard]>=0.30.0

# Auth (Story 1.3)
authlib>=1.3.0
python-jose[cryptography]>=3.3.0
itsdangerous>=2.1.0
# Pin exacto: evita que pip explore versiones (backtracking) en el build de Render.
httpx==0.28.1

# Secrets (Story 1.5)
google-cloud-secret-manager>=2.20.0

# Existing pipeline dependencies (shared)
requests>=2.31.0
python-dotenv>=1.0.0
python-dateutil>=2.9.0

# Beancount engine (Story 9.0+)
beancount>=3.2.0
# BQL queries + ledger file watcher (Story 9.2 — thin API)
beanquery>=0.2
watchfiles>=0.21
# Cartola PDF → directivas Beancount (Story 9.6a)
beangulp>=0.2

# Gemini SDK (Story 9.5 — PDF cartola extraction). Pin exacto (requiere httpx>=0.28.1).
google-genai==2.8.0

# Reporte de gastos para contadores (xlsx)
openpyxl>=3.1.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
