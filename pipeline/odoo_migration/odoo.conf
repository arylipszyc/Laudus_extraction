[options]
; Config del stack de migración (E1.1+). Necesario para que CUALQUIER invocación de
; `odoo` dentro del contenedor (incl. `docker compose exec`, que NO pasa por el
; entrypoint) conozca el host de la db — si no, Odoo intenta un socket local y falla.
admin_passwd = admin_e1_2026
db_host = db
db_user = odoo
db_password = odoo_pg_e1
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/mnt/extra-addons
