# Secretos y Entornos de Python
.env
venv/
env/
.venv/
serviceAccountKey.json

# Caches y Logs
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
*.log

# Datos generados de contabilidad y reportes locales
balance_sheet.json

# Frontend
node_modules/
dist/
build/
.next/

# Historiales locales de Chat
.claude/

# BMAD runtime cache (skills, templates — regenerable)
.agent/

# Sample cartolas locales (PII real — ver samples/README.md)
samples/*
!samples/README.md
!samples/.gitkeep

# Reportes generados por bootstrap/ (outputs del proceso, no source)
bootstrap/report-*.csv

# Validation runs de bootstrap/validate_cartolas_batch.py (Story 9.5c) — outputs locales
_bmad-output/validation-runs/

# Artefactos de runtime / cache (regenerables, no source)
*.picklecache
bootstrap/_cache/
# NOTA: ledger/_meta/import-log.jsonl ya NO se ignora — desde 5fec3b4 el importer
# lo commitea junto con los datos (el indicador de sync lo lee de origin); un
# archivo ignorado+untracked hacía fallar el `git add` de TODA corrida (12.4).

# Prototipos de reportes — outputs Excel generados localmente
_reportes-prototipo/*.xlsx

# Creds temporales del handoff Epic 9 (NUNCA commitear)
.handoff-creds.local
_handoff/

# Screenshots/artefactos efímeros del Playwright MCP
.playwright-mcp/

# Screenshots sueltos en la RAÍZ (scratch efímero) — los que valga conservar van a docs/screenshots/
/*.png
