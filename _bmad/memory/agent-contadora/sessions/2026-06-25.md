# Sesión 2026-06-25 — Creación de cuentas TC:Real (desbloqueo desglose TC 6.2)

## Qué pidió Ary
Definir y crear las cuentas que faltaban para desbloquear el desglose de TC (6.2): código
construido y testeado pero dormido por falta de cuentas en prod.

## Lo que hice
1. **Validé el diseño contra el código real** (no confié en mi memoria del 22-jun):
   - Las 9 cuentas-gasto TC (`Expenses:EAG:TC:*`, codes 43000X) **ya tienen `bank_account_id`**
     distinto cada una, incluidas las 3 USD (430006/08/10). → **Handoff item (1) ya estaba resuelto.**
   - Verifiqué el derivado exacto en `tc_correction.tc_real_account()`:
     `Expenses:EAG:TC:<stem>-<code>` → `Liabilities:EAG:TC:Real:<stem>` (sin -code) y
     `OPENING_EQUITY = "Equity:Apertura:TarjetasSinDetalle"`.
2. **Descubrí que el flujo 10.3 NO sirve** para estas cuentas (mi diseño §9 estaba mal):
   - `POST /cuentas-pendientes/{code}/promover` exige cuenta **en cuarentena** (descubierta en Laudus)
     → sintéticas dan 404; y **obliga categoria3 no-vacía** → choca con cat2/3 vacías (que es el
     mecanismo de exclusión del reporte). `_new-accounts-pending.beancount` además se regenera.
   - Decisión Ary: **declarar directo en `accounts.beancount`** (cuentas nuevas, regla de oro intacta).
3. **Creé las 9 cuentas** en `accounts.beancount` (bloque comentado al final). Codes sintéticos libres:
   TC:Real = espejo 2-prefix (230005/06/07/08/09/10/17/19); Equity = 311005. Metadata:
   cat1 no-vacía (PASIVO/PATRIMONIO), cat2/cat3 vacías, code sintético.
   - bean-check exit 0; 589 tests passed/1 xfailed/0 regresiones. Commit `1d6323b` (pusheado).
4. Corregí §9 del diseño (`valentina-correccion-tc-cartolas-2026-06-20.md`).

## Lo que falta para que el desglose corra end-to-end (no es contable, es de dev/Ary)
- **Wirear el categorizador real 9.7** en `_build_importer` (hoy `Noop`→`Suspense`). Sin esto las
  compras itemizadas caen todas en Suspense (safe, pero el reporte no las clasifica).
- **Importar una cartola 2026 real** por tarjeta para que la corrección se materialice. Es por tarjeta
  y por período; sin cartola, la TC queda como gasto lump en su cuenta original (estado interino OK).

## Aprendizaje
Verificar el flujo sancionado ANTES de prometerlo en el diseño. Asumí que 10.3 servía para cuentas
sintéticas; el endpoint está hecho para promover cuentas que Laudus descubre, con cat3 obligatoria.
Las cuentas inventadas por nosotros van directo al plan (declaración de contador), no por ese flujo.
