# Retrospectiva — Epic 6: Reconciliación Mensual

**Fecha:** 2026-06-25
**Facilitadora:** Amelia (Developer) · **Participantes:** Ary (Project Lead), Valentina (Contadora)
**Commit de cierre:** `e138a14`

## Resumen del epic

Reformulado bajo c4 (el motor 9.6b/9.10/9.12 ya existía; el epic fue wirear el SEAM del promote + cerrar el lazo de detección→anotación→cierre de período).

| Story | Entregable | Patches del review |
|-------|-----------|--------------------|
| 6.1 | Wiring promote→reconcile (modelo A: reconciliar sin postear) | 3 |
| 6.2 | Desglose TC USD/FX (orquestador `correct_tc_cartola`) | 3 |
| 6.3 | Aprobar diferencia → anotar en Beancount (`annotate_discrepancy`, atomicidad) | 3 |
| 6.4 | Completar dashboard de reconciliación (FR34) | 2 |
| 6.5b | Reconciliación period-aware (campo `year_month` persistido) | 0 |
| 6.5 | Cierre de período (FR35): run-record + `GET /periods` + indicador | 0 |

**6/6 stories `done`.** Todas pasaron por code-review de 3 capas (Blind + Edge + Acceptance Auditor). Tendencia clara: las últimas dos stories salieron limpias del review (0 patches).

## Lo que salió bien

1. **Patrón HALT-y-decidir.** En 6.1 el dev descubrió a mitad de implementación que postear las líneas de cartola duplicaba las cuentas que Laudus ya tenía → frenó y elevó la decisión en vez de adivinar. Ary eligió **modelo A (reconciliar sin postear)**, evitando un doble conteo que habría roto el cuadre peso-por-peso con el contador. El patrón se repitió sano en 6.2 (cuadre roto → cross-check vs BCCh) y 6.5b (limitación documentada aceptada en vez de gold-plating).
2. **Descubrimiento por datos reales.** Revisar `samples/` destapó que ~50% de las cartolas reales son TC internacionales en USD — algo fuera del plan original. Disparó 6.2 y la repriorización. Hallazgo asociado (Valentina): la TC *postea* el lump en Laudus, no matchea línea por línea como la cuenta corriente → su problema es de metadata, no de matching.
3. **Descomposición cuando el problema se reveló.** 6.5 se partió en 6.5b (period-aware) + 6.5 (cierre) al detectar que el motor bucketea por fecha de tx, no por período del estado. Hacer 6.5b primero hizo que el conteo de abiertas cuadrara con el run-record.

## Lo que costó / se arrastró

1. **2 tests rojos pre-existentes (`test_fava_edit_validator`)** viajaron como ruido a través de todas las stories; en cada review hubo que aclarar "son pre-existentes, 0 regresiones". Ensucia la señal. → **Action item #1.**
2. **Working tree acumulado sin commitear** a través de 6.5b + parches de 6.4 + 6.5 (tres capas sin commit hasta el cierre del epic). Funcionó por estar todo en `main`, pero es frágil. → **Acuerdo #2.**
3. **6.2 (TC) quedó con código vivo pero DORMIDO** por un handoff de prod no codeable. Es la feature con más valor contable del epic (itemizar compras vs el lump) esperando datos sembrados a mano. → **Critical path #3.**

## Action items

### Proceso / Calidad
1. **Arreglar o quitar los 2 tests rojos `test_fava_edit_validator`.** Owner: dev (próxima sesión). ✔ cuando `PYTHONUTF8=1 venv/Scripts/python.exe -m pytest backend/tests -q` da 0 failed.
2. **Commitear al cerrar cada story** (no acumular working tree multi-story). Acuerdo de equipo.

### Critical path — desbloquear el desglose de TC (6.2) en prod
3. Handoff de prod de TC (hoy bloqueante de que el código ya construido/testeado corra):
   - Sembrar `bank_account_id` en las cuentas-gasto `...Us`.
   - Sembrar las 8 `TC:Real:*` + `Equity:Apertura:TarjetasSinDetalle` vía el flujo 10.3.
   - Wirear el categorizador real 9.7 en `_build_importer` (hoy `Noop`→`Suspense`) — codeable.
   Owner: Ary (siembra de datos) + dev (wire 9.7).

## Defers de los code-reviews del epic (referencia)

Registrados en `deferred-work.md`. Notables: `reconciled_at` lexicográfico en `list_periods` (correcto hoy, hardening low-pri); runs JSONL append-only O(n) por `/periods`; padding ±tol contamina el matcher más allá de `missing-in-cartola` (limitación documentada aceptada en 6.5b); `year_month=period.end` colapsa estados multi-mes (latente, cartolas son mensuales); boundary write/git de `commit_reconciliation` (lost-update concurrente + retry duplica, clase pre-existente de 6.1/6.3).

## Próximo epic / preparación

- **Epic 7 (Colaboración Owner-Contador)** está en `backlog`, sin stories detalladas y sin dependencia dura de Epic 6 más allá del dashboard que ya existe.
- **Corrección 2026-06-25 (post-retro):** el flip del balance-sheet ya estaba HECHO desde 2026-06-19 (stories 9.15 flip + 9.16 cleanup). `dashboard/service.py` ya no tiene rama de flag: lee `balance_sheet_via_beancount` siempre; el path Sheets fue removido. Los dashboards Activos/Pasivos ya leen Beancount consolidado en prod. La nota original de esta retro ("falta tirar el flag") era stale — corregida. Lo único pendiente del balance-sheet son items operativos no-codeables de Ary: smoke de `BalanceSheetPage`, avisar a la family (TC→Pasivos), y Sheets→read-only en Drive (AC5 de 9.11).
- **Trabajo de mayor valor pendiente real (cross-epic):** desbloquear el desglose de TC dormido (critical path #3) — es lo que queda con valor contable construido y sin liberar.

## Hallazgo significativo

El desglose de TC (6.2) está **implementado pero dormido** por el handoff de prod. No bloquea Epic 7, pero es valor contable construido y sin liberar. Es el critical path #3.
