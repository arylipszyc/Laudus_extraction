# Deferred Work

Trabajo real pero no bloqueante, diferido con su razón. Revisar al retomar el epic/story asociado.

## Deferred from: code review of E1-0-scaffold-pipeline-migracion (2026-07-24)

- **[Smoke Tier B] El smoke escribe el stub en la carpeta de addons compartida del spike** — `pipeline/odoo_migration/tests/test_odoo_smoke.py`. El compose del spike monta `./addons:/mnt/extra-addons` (bind mount), así que `odoo scaffold x_laudus_smoke /mnt/extra-addons` escribe en `_spike-odoo/addons/x_laudus_smoke` (dir compartido con el stack del spike), y en Linux los archivos root-owned harían fallar el `shutil.rmtree` de teardown (tragado por `ignore_errors=True`) dejando residuo. El aislamiento por project-name cubre volúmenes, no este bind mount del host. Impacto bajo: Tier B es opt-in (no corre por-commit) y se auto-limpia en el happy path. **Razón del defer:** el README ya declara que **E1.1 promoverá su propio compose** cuando monte el addon real — natural resolver el teardown/mount aislado ahí en vez de endurecer un compose que se va a reemplazar.

## Deferred from: code review of E1-1-modulo-odoo-x-laudus-migration (2026-07-24)

- **[Hook] `del registry._auto_install_template` depende de un interno privado de Odoo + tag flotante `odoo:18`** — `pipeline/odoo_migration/addons/x_laudus_migration/hooks.py`. Si un point release de Odoo 18 renombra/mueve ese mecanismo, el guard `hasattr` convierte la cancelación en no-op silencioso y el CoA genérico se cargaría (borrando LAU1). Mitigado: la regresión la pescan los tests Tier B (conteo de diarios; el patch de asserts del review propone además un assert directo `count(account_account)==0`). Pinnear la imagen (digest/tag fechado) es una decisión de stack que afecta también al compose del spike — resolver cuando se levante el VPS (entorno definitivo).
- **[Smoke Tier B] `up -d` fallido → `pytest.skip` enmascara causas reales** — `pipeline/odoo_migration/tests/test_x_laudus_migration_install.py`. Un fallo de `docker compose up` (puerto 8070 ocupado, compose inválido, pull fallido) se reporta como skip "¿daemon Docker abajo?" y el gate de release no corre pareciendo verde. Mismo patrón ya adjudicado en el smoke de E1.0 (`test_odoo_smoke.py`) — si se endurece, endurecer ambos a la vez (skip solo si docker no está / daemon no responde; fail en el resto).

## Deferred from: code review of E1-2-transformador-colapso-verificador-paridad (2026-07-25)

- ~~**[Parity] Exclusiones por CONTEO, no por identidad**~~ — **CERRADO en E1.3 (2026-07-25):** `verify_counts`/`run_tier_a` ahora reciben `excluded_je_ids={(company, je_id), …}` y verifican identidad exacta + neteo a 0 por código del conjunto excluido; excluir el par equivocado / una pata suelta / sin declarar falla con alarma (tests de mutación en `test_parity_tier_a.py` y `test_sinceramiento.py`).
- ~~**[Transform] `OdooLineRecord` no conserva `posting.price`**~~ — **CERRADO en E1.5 (2026-07-25):** `OdooLineRecord.price` (default `None`) poblado desde `posting.price.number` en `collapse`; el builder (`load.py`) deriva el contravalor CLP como `amount × price` y falla fuerte ante una pata no-CLP sin precio (`test_mutacion_usd_sin_price_acusa`).
- **[Parity] `ParityError` sin diffs programáticos** — `pipeline/odoo_migration/parity.py` (`run_tier_a` trunca el mensaje a 5 reprs; los diffs completos no viajan como atributo de la excepción). En un descuadre real de cientos de códigos el operador no puede recuperar la lista completa. **Razón del defer:** DX de diagnóstico, no correctitud; resolver cuando el verificador corra a escala (**E1.3/E1.6**) adjuntando `origin_diffs`/`destination_diffs` a la excepción.
- ~~**[Mapping] Consistencia `(company, odoo_account) → otype` no validada**~~ — **CERRADO en E1.5 (2026-07-25):** `build_chart` (`load.py`) valida al construir el chart: `otype` inconsistente entre códigos que colapsan al mismo destino → `LoadError` con contexto (tests de mutación en `test_load_payloads.py`).

## Deferred from: code review of E1-3-transformador-sinceramiento-naturalezas (2026-07-25)

- **[Parity] `_txn_identity` asume company homogénea por txn** — `pipeline/odoo_migration/parity.py` (deriva la company de `postings[0]`; sin guard de homogeneidad). Un txn con postings de entidades que mapeen a companies distintas daría identidad inconsistente con `(m.company, m.je_id)` del move → falsos "SIN declarar"/"fantasma". **Razón del defer:** inalcanzable hoy — cada JE de Laudus vive en un solo RUT (EAG+hijas → EAG; FFCC/JAB → RUT2); guard de homogeneidad = hardening para cuando cambie la fuente.
- **[Sincerar] Cierre de población keyed por prefijo `Income:` del destino** — `pipeline/odoo_migration/sincerar.py` (`classify_line`: `sinc` vacío + destino no-`Income:` → None silencioso). Una futura fila de ingreso ya re-ruteada a nivel tabla con `sinc` vacío escaparía del sinceramiento sin fail-loud. **Razón del defer:** población cerrada hoy (83 filas con `sinc` cross-checkeadas por tests); no hay señal confiable de "origen-ingreso" fuera de la tabla — resolver si la tabla incorpora una columna de tipo Laudus.
- **[Parity] Neteo de exclusiones agregado, no por par declarado** — `pipeline/odoo_migration/parity.py` (`verify_counts` netea el set excluido completo). Dos moves reales no relacionados que neteen por (company, code, currency) pasarían el gate. **Razón del defer:** la API de set plano la pinneó la propia story (E1.3) y `sincerar` solo produce sets derivados de pares completos; hardening de API para callers futuros (E1.4+) que declaren exclusiones a mano.
- **[Sincerar] D-1: residual Latinoamericana FFCC-310009 ±423,6M no excluido en el mirror real** — grupo de 4 moves (RUT2 1194/1196/1316/1321) reversado por UNO (5387): el emparejamiento por pares no puede 4-vs-1; las 10 patas quedan en Income + `latam-sin-par`/revisar (netean 0, nada pinneado se mueve; el inventario N-1 dice "Excluir"). **Razón del defer (decisión Ary 2026-07-25):** esperar el veredicto de Valentina — si confirma exclusión, implica extender el algoritmo a grupos-que-netean (mueve conteos pinneados); resolver antes de E1.5.

## Deferred from: implementation of E1-5-loader-idempotente-odoo (2026-07-25)

- ~~**[Loader] Ventana de crash entre `create` del lote y el registro de xmlids**~~ — **CERRADO en el code review de E1.5 (2026-07-25, P2/P3):** `load_all` ahora ADOPTA los huérfanos por clave natural en TODOS los payloads (cuenta: code+cía; partner: nombre; analítica: nombre+plan; move: `x_laudus_je_id`+cía con re-verificación de contenido línea a línea) en vez de duplicar o chocar contra un unique, y un barrido final postea los moves del payload que quedaron en draft (`moves_reposted`) — la ventana registro→post tampoco deja borradores permanentes.
- **[Loader] `upsert` = skip-si-existe, sin detección de drift** — si los transformadores cambian (nueva regla E1.3/E1.4), re-correr sobre una db ya cargada deja los moves viejos como están (skip silencioso). Disciplina documentada (docstring `loader_rpc.py` + README): el loader es para cargas frescas / reanudación; re-sincronizar tras cambio de reglas = db nueva. **Razón del defer:** el delta repetible fino es Fase 2 / decisión §5 del brief (winston §7.3); la verificación post-carga real es E1.6.

## Deferred from: code review of E1-5-loader-idempotente-odoo (2026-07-25)

- **[Loader/CLI] Credenciales y TLS para la corrida VPS** — `pipeline/odoo_migration/loader_rpc.py` (`--password` default `admin` viaja en argv — visible en process list/shell history — y el cliente habla `http://` plano). Hoy el CLI solo apunta al compose dev local. **Razón del defer:** el full load real es post-E1.6 con VPS provisto; resolver en ese runbook (password por env-var, https obligatorio contra el VPS).
- **[Loader] Escala del apareo de xmlids: `_xmlid_map` sin paginar + 1 `search` por move** — `pipeline/odoo_migration/loader_rpc.py` (`_xmlid_map` trae TODO el namespace `__laudus__` en una respuesta XML-RPC — cientos de miles de `aml_*` en el full — y la fase de registro hace un roundtrip por move; `BATCH_SIZE` solo cubre `create`). **Razón del defer:** winston §9 ya manda medir 1 año ANTES del full load; paginar/optimizar ahí con números reales.
- **[Smoke Tier B] `compose exec odoo odoo -i` corre un segundo proceso Odoo contra la db del server vivo** — `pipeline/odoo_migration/tests/test_loader_idempotente.py` (ventana de invalidación de registry/cache = fuente de flake que `_wait_for_odoo` enmascara). Patrón E1.1 verbatim (pre-existente). **Razón del defer:** endurecer junto con los smokes E1.0/E1.1 (defer ya registrado arriba) — no divergir del patrón en una sola story.

## Deferred from: code review of E1-4-transformador-dimensiones-partners (2026-07-25)

- ~~**[Dimensionar] `otype` sin población cerrada — un typo apaga la regla beneficiario en silencio**~~ — **CERRADO en E1.5 (2026-07-25):** `OTYPE_TO_ACCOUNT_TYPE` (`load.py`) es la población cerrada de los 7 valores reales del CSV; un `otype` vacío/desconocido revienta `build_chart` con contexto (el chart se construye de la tabla COMPLETA, así que cualquier typo en cualquier fila se pesca aunque la fila no participe del golden).
- **[Sincerar] Claves duplicadas en el YAML de alias se pisan en silencio** — `pipeline/odoo_migration/sincerar.py` (`yaml.safe_load` conserva la última entrada ante un nombre duplicado tras un merge del YAML → aliases perdidos y under-match sin alarma). **Razón del defer:** pre-existente de E1.3 (`load_alias_table` sin guard de duplicados); endurecer con un loader que acuse duplicados cuando se toque esa función de nuevo.
- **[Tests] `full_mirror_chain` session-scoped comparte moves/lines mutables entre módulos** — `pipeline/odoo_migration/tests/conftest.py` (protegido solo por docstring; un test futuro que mute en sitio envenenaría otros módulos con fallos dependientes del orden). **Razón del defer:** higiene de tests, no correctitud — los tests de mutación ya trabajan sobre corridas frescas de `dimensionar`; congelar el fixture es costoso frente al riesgo actual.
