;; ledger/opening-2021.beancount — saldos iniciales al 2021-01-01.
;;
;; ⚠️ INTENCIONALMENTE VACÍO desde 2026-06-19.
;;
;; Originalmente (Story 9.1) este archivo seteaba la apertura con pares
;; `pad`/`balance` contra Equity:EAG:OpeningBalances, derivados del balance sheet
;; de Laudus (JE 140 — "Saldo anterior"). Pero el importer Laudus (Story 9.4)
;; ya trae ESE MISMO JE 140 como la transacción "Saldo inicial" en
;; `imports/laudus/2021-01.beancount` → la apertura quedaba CONTADA DOS VECES
;; (~727M CLP fantasma en 12 cuentas; ver docs/handoff-epic9-2026-06-18.md).
;;
;; Fuente canónica = Laudus ("Laudus prima"): la apertura vive en el JE 140 del
;; importer, que es self-balancing vía Liabilities:EAG:Apertura-211005 y se
;; regenera consistente en cualquier backfill. Se removieron los pad/balance del
;; bootstrap para que la apertura quede contada UNA sola vez.
;;
;; ⚠️ NO re-correr `bootstrap/generate_opening_balances.py` apuntando a este
;; archivo sin reintroducir el doble — el bootstrap quedó superseded por el
;; importer Laudus.
